Upwards exposed: 
Uses for each definition of a var v, the set of points where v is used.
For each definition of a variable, find all uses that it reaches.


javac -d test test/*/Test.java ;
javac -cp soot.jar -d class ./src/*.java
java -cp ../soot.jar:. RunDataFlowAnalysis case2.Test



